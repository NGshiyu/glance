interface:
  display_name: "Glance"
  short_description: "Repo-specific patterns and workflows for glance"
  default_prompt: "Use the glance repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true